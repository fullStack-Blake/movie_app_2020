import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

//(<App> is JSX) JSX only use one component at a time
// JSX is specific for react only
// Component - just a function returns html
ReactDOM.render(<App />, document.getElementById("root"));
